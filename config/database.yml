default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("DB_HOST", "db") %>
  username: <%= ENV.fetch("DB_USER", "postgres") %>
  password: <%= ENV.fetch("DB_PASSWORD", "password") %>

development:
  <<: *default
  database: <%= ENV.fetch("DB_NAME", "movie_reservation_system_development") %>

test:
  <<: *default
  database: movie_reservation_system_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
  database: <%= ENV.fetch("DB_NAME", "movie_reservation_system_production") %>
  connects_to:
    database: { writing: :production, reading: :production }
    cache: :cache

# ✅ Added for Rails 8 SolidCache
cache:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
  database: solid_cache_production

# ✅ For Action Cable (WebSockets)
cable:
  <<: *default
  database: <%= ENV.fetch("DB_NAME", "movie_reservation_system_cable") %>

# ✅ For SolidQueue (Background Jobs)
queue:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
  database: <%= ENV.fetch("DB_NAME", "movie_reservation_system_queue") %>
